import { Drawer } from "@mui/material"
import React from "react"

export default () => {
    return <Drawer
        variant="persistent"
        open={true}
        anchor="right"
    >
        TOOLBAR IS EMPTY
    </Drawer>
}